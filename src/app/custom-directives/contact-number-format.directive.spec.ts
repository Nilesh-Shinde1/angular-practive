import { ContactNumberFormatDirective } from './contact-number-format.directive';

describe('ContactNumberFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new ContactNumberFormatDirective();
    expect(directive).toBeTruthy();
  });
});
